<nav class="navbar navbar-expand-lg bg-danger">
  <div class="container-fluid">
    <a class="navbar-brand fw-bold text-warning" href="/">PROJECT <sup>59</sup></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-links nav-link fw-bolder" class="{{#if (equal currenPage 'index')}} active active-link {{/if}}" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-links nav-link fw-bolder" class="{{#if (equal currenPage 'project')}} active active-link {{/if}}" href="/project">My Project</a>
        </li>
          <li class="nav-item">
          <a class="nav-links nav-link fw-bolder" class="{{#if (equal currenPage 'testimonial')}} active active-link {{/if}}" href="/testimonial">Testimonials</a>
        </li>
      </ul>
    </div>
  </div>
  {{#if user }}
        <p style="width: 170px;" class="mt-3 fw-bolder text-light text-end p-1 rounded me-2">Hello, {{user.username}}!</p>
        <a class="logout-item p-2 rounded fw-bolder" href="/logout">Logout</a>
  {{else}}
  <main class="d-flex">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="login nav-link rounded me-2 fw-bolder" href="/login">Login</a>
      </li>
      <li class="nav-item">
        <a class="register nav-link rounded fw-bolder" href="/register">Register</a>
      </li>
  {{/if}}
    </ul>
    <ul class="navbar-nav me-3">
      <li class="nav-item">
        <a class="contact-me nav-link shadow-lg fw-bolder rounded ms-2" href="/contactForm">Contact</a>
      </li>
    </ul>
  </main>
</nav>